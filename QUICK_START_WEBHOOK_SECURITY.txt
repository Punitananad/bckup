═══════════════════════════════════════════════════════════════
  🔒 WEBHOOK SECURITY - QUICK START GUIDE
═══════════════════════════════════════════════════════════════

✅ CODE CHANGES: COMPLETE
🔴 YOUR ACTION REQUIRED: Add webhook secrets and restart

═══════════════════════════════════════════════════════════════
  STEP 1: GET WEBHOOK SECRETS (5 min)
═══════════════════════════════════════════════════════════════

1. Open: https://dashboard.razorpay.com/
2. Go to: Settings → Webhooks
3. Copy webhook secret (starts with "whsec_")

═══════════════════════════════════════════════════════════════
  STEP 2: ADD TO ADMIN PANEL (2 min)
═══════════════════════════════════════════════════════════════

1. Open: https://calculatentrade.com/admin/
2. Go to: AdminPortal → Payment gateways
3. Edit "Razorpay":
   - Gateway salt: [paste webhook secret]
   - Save
4. Edit "split_razorpay":
   - Gateway salt: [paste webhook secret]
   - Save

═══════════════════════════════════════════════════════════════
  STEP 3: RESTART SERVICES (1 min)
═══════════════════════════════════════════════════════════════

sudo systemctl restart gunicorn daphne

═══════════════════════════════════════════════════════════════
  STEP 4: TEST (5 min)
═══════════════════════════════════════════════════════════════

# Monitor logs:
sudo journalctl -u gunicorn -f

# Make a test payment
# Look for: ✅ Razorpay webhook verified

═══════════════════════════════════════════════════════════════
  WHAT CHANGED IN CODE
═══════════════════════════════════════════════════════════════

File: application/scan2food/theatre/api_views.py

1. Added import:
   from .webhook_security import verify_webhook_request

2. Updated razporpay_webhook_url():
   - Verifies signature BEFORE processing payment
   - Returns 401 if signature invalid

3. Updated split_razporpay_webhook_url():
   - Verifies signature BEFORE processing payment
   - Returns 401 if signature invalid

═══════════════════════════════════════════════════════════════
  SECURITY BEHAVIOR
═══════════════════════════════════════════════════════════════

✅ Correct secret  → Payment SUCCESS
❌ Wrong secret    → Payment FAILS (401 error)
❌ No secret       → Payment FAILS (401 error)
❌ Fake webhook    → Payment FAILS (signature mismatch)

═══════════════════════════════════════════════════════════════
  IMPORTANT NOTES
═══════════════════════════════════════════════════════════════

• Webhook Secret ≠ API Secret (they are different!)
• No .env file needed (everything in database)
• Uses existing gateway_salt field (no new column)
• Strict security (no backward compatibility)

═══════════════════════════════════════════════════════════════
  TROUBLESHOOTING
═══════════════════════════════════════════════════════════════

If payment fails:
1. Check webhook secret is correct in admin panel
2. Check logs: sudo journalctl -u gunicorn -f
3. Look for error messages
4. Verify webhook URL in Razorpay dashboard

If services don't start:
1. Check logs: sudo journalctl -u gunicorn -n 50
2. Check syntax: python manage.py check
3. Check ports: sudo netstat -tulpn | grep -E '8000|8001'

═══════════════════════════════════════════════════════════════
  DOCUMENTATION FILES
═══════════════════════════════════════════════════════════════

📄 NEXT_STEPS_WEBHOOK_SECURITY.md     ← START HERE
📄 IMPLEMENTATION_SUMMARY.md          ← Full details
📄 WEBHOOK_SECURITY_COMPLETE.md       ← Technical details
📄 SIMPLE_IMPLEMENTATION_GATEWAY_SALT.md ← Step-by-step

═══════════════════════════════════════════════════════════════
  TOTAL TIME: ~15 MINUTES
═══════════════════════════════════════════════════════════════

Code changes: ✅ DONE
Your actions:  🔴 PENDING (add secrets + restart)

═══════════════════════════════════════════════════════════════
